# Get these from https://api.slack.com/apps → your app
# OAuth credentials (required for multi-workspace)
SLACK_CLIENT_ID=your-client-id
SLACK_CLIENT_SECRET=your-client-secret
SLACK_SIGNING_SECRET=your-signing-secret

PORT=3000
DASHBOARD_URL=http://localhost:3000   # public URL (used for OAuth redirects and App Home links)
DATA_DIR=                             # optional: stable path for SQLite DB (e.g. /home/forge/data)
